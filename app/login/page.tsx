"use client";import Link from"next/link";import{useRouter}from"next/navigation";import{FormEvent,useState}from"react";import{Shell}from"../components";import{hashListenerPassword,readListenerAccount,saveListenerSession}from"../listener-account";import{useLanguage}from"../i18n/language-provider";import{listenerAuthCopy}from"../listener-auth-copy";
export default function Login(){const router=useRouter(),{locale}=useLanguage(),t=listenerAuthCopy[locale],[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),email=String(f.get("email")||"").trim().toLowerCase(),password=String(f.get("password")||""),account=readListenerAccount();if(!account||account.email!==email||account.passwordHash!==await hashListenerPassword(password)){setError(t.badLogin);return}saveListenerSession({email:account.email,displayName:account.displayName});router.push("/library")}return <Shell player={false}><section className="page-wrap listener-auth"><form onSubmit={submit} className="listener-auth-card"><p className="page-kicker">{t.welcome}</p><h1>{t.loginTitle}</h1><p>{t.loginText}</p><label>{t.email}<input name="email" type="email" autoComplete="email" required/></label><label>{t.password}<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p className="listener-auth-error" role="alert">{error}</p>}<button className="m-primary" type="submit">{t.signIn}</button><p>{t.newHere} <Link href="/signup">{t.create}</Link></p></form></section></Shell>}