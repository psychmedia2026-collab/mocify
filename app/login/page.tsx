"use client";import Link from"next/link";import{useRouter}from"next/navigation";import{FormEvent,useState}from"react";import{Shell}from"../components";import{hashListenerPassword,readListenerAccount,saveListenerSession}from"../listener-account";
export default function Login(){const router=useRouter(),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),email=String(f.get("email")||"").trim().toLowerCase(),password=String(f.get("password")||""),account=readListenerAccount();if(!account||account.email!==email||account.passwordHash!==await hashListenerPassword(password)){setError("Email or password is incorrect.");return}saveListenerSession({email:account.email,displayName:account.displayName});router.push("/library")}return <Shell player={false}><section className="page-wrap listener-auth"><form onSubmit={submit} className="listener-auth-card"><p className="page-kicker">WELCOME BACK</p><h1>Listener login</h1><p>Sign in to your MOCIFY account.</p><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label>{error&&<p className="listener-auth-error">{error}</p>}<button className="m-primary" type="submit">Sign in</button><p>New here? <Link href="/signup">Create listener account</Link></p></form></section></Shell>}