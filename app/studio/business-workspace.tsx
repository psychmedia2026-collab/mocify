"use client";
import Link from"next/link";import{useEffect,useState}from"react";import{ArtistPortalShell}from"../artist-components";import ArtistDashboardLayout,{type ArtistPlanTier}from"../artist-dashboard-layout";import styles from"./business-workspace.module.css";
type BusinessSection="analytics"|"earnings"|"promote";const meta={analytics:{icon:"◫",title:"Analytics",sub:"Bekijk wie luistert, waar je publiek zit en hoe je muziek groeit."},earnings:{icon:"€",title:"Earnings",sub:"Volg royalty's, saldo en je uitbetalingsgeschiedenis."},promote:{icon:"↗",title:"Promote",sub:"Promoot je muziek met MOCIFY Promo Credits. Campagnes worden apart van je abonnement betaald."}}as const;
export default function BusinessWorkspace({section}:{section:BusinessSection}){const[plan,setPlan]=useState<ArtistPlanTier|null>(null);const[toast,setToast]=useState("");useEffect(()=>{const saved=localStorage.getItem("mocify-artist-plan");const normalized:ArtistPlanTier=saved==="pro"?"pro":saved==="max"||saved==="studio"?"max":"free";setPlan(normalized)},[]);const notify=(t:string)=>{setToast(t);setTimeout(()=>setToast(""),2200)};if(plan===null)return null;const access=plan==="pro"||plan==="max";const current=meta[section];return <ArtistPortalShell active="studio"><section className={`page-wrap ${styles.dashboardPage}`}><ArtistDashboardLayout plan={plan}><div className={styles.businessMain}><header className={styles.hero}><div><p>{plan==="max"?"ARTIST MAX":plan==="pro"?"ARTIST PRO":"ARTIST FREE"}</p><h1><span>{current.icon}</span>{current.title}</h1><div>{current.sub}</div></div></header>{!access?<UpgradeGate section={section}/>:<>{section==="analytics"&&<Analytics/>}{section==="earnings"&&<Earnings notify={notify}/>} {section==="promote"&&<Promote notify={notify}/>}</>}</div></ArtistDashboardLayout></section>{toast&&<div className={styles.toast}>{toast}</div>}</ArtistPortalShell>}
function UpgradeGate({section}:{section:BusinessSection}){return <section className={styles.upgradeGate}><small>ARTIST FREE · VERGRENDELD</small><h2>{section==="promote"?"Promote is beschikbaar vanaf Artist Pro":`${section} is onderdeel van Artist Pro`}</h2><p>Upgrade naar Artist Pro of Artist Max om deze functie te gebruiken.</p><div className={styles.gateActions}><Link className={styles.primary} href="/for-artists">Bekijk Artist Pro</Link><Link href="/dashboard">Terug naar Dashboard</Link></div></section>}
function Analytics(){return <div className={styles.stats}>{[["24.8K","Streams"],["8.2K","Listeners"],["1,284","Saves"],["€184","Estimated earnings"]].map(x=><article key={x[1]}><small>{x[1]}</small><strong>{x[0]}</strong></article>)}</div>}
function Earnings({notify}:{notify:(s:string)=>void}){return <div className={styles.balance}><div><small>AVAILABLE BALANCE</small><strong>€184.32</strong><span>Next estimated update · 30 Sep</span></div><button onClick={()=>notify("Payout request prepared.")}>Request payout</button></div>}
function Promote({notify}:{notify:(s:string)=>void}){const[goal,setGoal]=useState("Streams");const[ownedCredits,setOwnedCredits]=useState(1250);const[pendingCredits,setPendingCredits]=useState(0);const[cost,setCost]=useState(500);const add=(n:number)=>setPendingCredits(p=>p+n);const remove=(n:number)=>setPendingCredits(p=>Math.max(0,p-n));const confirm=()=>{if(!pendingCredits)return;setOwnedCredits(c=>c+pendingCredits);notify(`${pendingCredits.toLocaleString("nl-NL")} Promo Credits toegevoegd aan je saldo (prototype).`);setPendingCredits(0)};const launch=()=>{if(ownedCredits<cost){notify("Onvoldoende gekochte Promo Credits.");return}setOwnedCredits(c=>c-cost);notify(`Campagne gestart. ${cost} Promo Credits gebruikt.`)};return <><section className={styles.creditWallet}><div><small>JOUW PROMO CREDITS</small><strong>{ownedCredits.toLocaleString("nl-NL")}</strong><span>Al in bezit · dit saldo kun je niet handmatig verlagen</span></div><div><small>NIEUWE CREDITS TOEVOEGEN</small><strong>+ {pendingCredits.toLocaleString("nl-NL")}</strong><span>Pas dit aantal aan vóór bevestigen</span><div className={styles.creditBuy}>{[500,1500,5000].map(n=><div key={n}><button onClick={()=>remove(n)} disabled={pendingCredits===0}>− {n.toLocaleString("nl-NL")}</button><button onClick={()=>add(n)}>+ {n.toLocaleString("nl-NL")}</button></div>)}</div><button className={styles.primary} disabled={pendingCredits===0} onClick={confirm}>Credits toevoegen</button></div></section><div className={styles.two}><section className={styles.card}><small>NEW CAMPAIGN</small><h2>Push your next release</h2><label>Goal<div className={styles.choices}>{["Streams","Followers","Saves"].map(x=><button onClick={()=>setGoal(x)} className={goal===x?styles.selected:""} key={x}>{x}</button>)}</div></label><label>Campaign budget<select value={cost} onChange={e=>setCost(+e.target.value)}><option value={250}>250 Promo Credits</option><option value={500}>500 Promo Credits</option><option value={1000}>1.000 Promo Credits</option><option value={2500}>2.500 Promo Credits</option></select></label><div className={styles.costLine}><span>Campaign cost</span><b>{cost.toLocaleString("nl-NL")} credits</b></div><button className={styles.primary} onClick={launch}>{ownedCredits<cost?"Buy more credits":"Launch campaign"}</button></section><section className={styles.card}><small>HOW IT WORKS</small><h2>Promo Credits</h2><p className={styles.creditText}>Je bestaande saldo staat vast. Met + kies je hoeveel nieuwe credits je wilt toevoegen. Heb je vóór het bevestigen te veel gekozen, dan verlaag je alleen dat nieuwe aantal met −. Je bestaande credits worden nooit door de min-knop verminderd.</p></section></div></>}
