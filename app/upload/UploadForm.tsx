"use client";
import {useState}from"react";import{genres}from"../data";const MAX_FILE_SIZE=15*1024*1024,MAX_DURATION_SECONDS=5*60;
export default function UploadForm(){const[fileName,setFileName]=useState("");const[fileInfo,setFileInfo]=useState("");const[fileError,setFileError]=useState("");async function handleFileChange(event:React.ChangeEvent<HTMLInputElement>){const input=event.currentTarget,file=input.files?.[0];setFileName("");setFileInfo("");setFileError("");if(!file)return;const isMp3=file.type==="audio/mpeg"||file.name.toLowerCase().endsWith(".mp3");if(!isMp3){setFileError("Only MP3 files are allowed.");input.value="";return}if(file.size>MAX_FILE_SIZE){setFileError("This MP3 is larger than 15 MB.");input.value="";return}const duration=await getAudioDuration(file);if(duration===null){setFileError("MOCIFY could not read this MP3. Please choose another file.");input.value="";return}if(duration>MAX_DURATION_SECONDS+.5){setFileError("Tracks can be up to 5:00 minutes long.");input.value="";return}setFileName(file.name);setFileInfo(`${formatDuration(duration)} · ${formatMegabytes(file.size)} MB`)}return <form className="rounded-xl border border-white/10 bg-[radial-gradient(circle_at_90%_0%,rgba(237,53,197,.12),transparent_28%),linear-gradient(145deg,#0e101c,#090b15)] p-6" aria-describedby="upload-prototype-note"><div className="mb-4 grid h-12 w-12 place-items-center rounded-xl border border-fuchsia-500/40 bg-gradient-to-br from-violet-600/30 to-fuchsia-500/20 text-2xl text-fuchsia-400">↑</div><p className="text-[9px] font-black tracking-[.14em] text-fuchsia-400">UPLOAD MASTER</p><h2 className="mt-1 text-2xl font-black tracking-[-.04em]">Upload a track</h2><p id="upload-prototype-note" className="mt-2 text-xs leading-5 text-zinc-500">Prototype flow — storage and publishing will be connected in the backend phase.</p><div className="mt-6 grid gap-4 md:grid-cols-2"><Field label="Track title" text="Enter track title"/><Field label="Artist name" text="Your artist name"/><label className="text-[10px] font-bold text-zinc-300">Genre<select name="genre" defaultValue="" className="mt-2 block w-full rounded-lg border border-white/10 bg-black/30 px-4 py-3 font-normal text-zinc-300"><option value="" disabled>Choose genre</option>{genres.map(genre=><option value={genre} key={genre}>{genre}</option>)}</select></label><Field label="AI creation tool" text="Suno, Udio or other"/></div><label className="relative mt-4 block cursor-pointer rounded-xl border border-dashed border-violet-400/40 bg-violet-500/5 px-6 py-9 text-center focus-within:outline-2 focus-within:outline-cyan-400"><span className="text-xl text-fuchsia-400">✦</span><span className="mt-2 block text-xs font-bold">Choose your MP3</span><span id="audio-formats" className="mt-2 block text-[10px] text-zinc-500">MP3 only · max. 5:00 min · max. 15 MB</span>{fileName&&<span className="selected-file" aria-live="polite">Selected: {fileName}<small className="mt-1 block font-normal text-zinc-400">{fileInfo}</small></span>}{fileError&&<span className="mt-3 block text-xs font-bold text-rose-300" role="alert">{fileError}</span>}<input type="file" name="audio" accept=".mp3,audio/mpeg" aria-describedby="audio-formats" className="absolute inset-0 h-full w-full cursor-pointer opacity-0" onChange={handleFileChange}/></label><button type="button" disabled title="Publishing is coming in the backend phase" className="prototype-control mt-5 w-full rounded-lg border border-fuchsia-400/50 bg-gradient-to-r from-violet-600 to-fuchsia-500 px-4 py-3 text-xs font-black text-white">Continue <span className="prototype-badge">SOON</span></button></form>}
function Field({label,text}:{label:string;text:string}){return <label className="text-[10px] font-bold text-zinc-300">{label}<input name={label.toLowerCase().replaceAll(" ","-")} placeholder={text} className="mt-2 block w-full rounded-lg border border-white/10 bg-black/30 px-4 py-3 font-normal text-zinc-300 placeholder:text-zinc-600"/></label>}
function getAudioDuration(file:File):Promise<number|null>{return new Promise(resolve=>{const url=URL.createObjectURL(file),audio=new Audio();const finish=(value:number|null)=>{URL.revokeObjectURL(url);resolve(value)};audio.preload="metadata";audio.onloadedmetadata=()=>finish(Number.isFinite(audio.duration)?audio.duration:null);audio.onerror=()=>finish(null);audio.src=url})}function formatDuration(seconds:number){const rounded=Math.round(seconds),minutes=Math.floor(rounded/60),rest=String(rounded%60).padStart(2,"0");return`${minutes}:${rest}`}function formatMegabytes(bytes:number){return(bytes/(1024*1024)).toFixed(1)}
