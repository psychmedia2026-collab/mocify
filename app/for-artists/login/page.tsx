"use client";
import Link from"next/link";import{useRouter}from"next/navigation";import{useState,type FormEvent}from"react";import{ArtistPortalShell}from"../../artist-components";import{useLanguage}from"../../i18n/language-provider";import{artistTranslations,authTranslations}from"../../i18n/artist-translations";import{hashArtistPassword,readArtistAccount,writeArtistSession}from"../../artist-access";
const messages={en:{missing:"No Artist account exists in this browser yet. Create an account first.",invalid:"Email or password is incorrect.",working:"Checking...",note:"Prototype login: credentials are checked against the Artist account created on this device. Production authentication will later move to the server."},nl:{missing:"Er bestaat in deze browser nog geen Artist-account. Maak eerst een account aan.",invalid:"E-mailadres of wachtwoord is onjuist.",working:"Controleren...",note:"Prototype-login: de gegevens worden gecontroleerd tegen het Artist-account dat op dit apparaat is aangemaakt. Productie-authenticatie verhuist later naar de server."},ro:{missing:"Nu există încă un cont Artist în acest browser. Creează mai întâi un cont.",invalid:"Adresa de e-mail sau parola este incorectă.",working:"Se verifică...",note:"Autentificare prototip: datele sunt verificate cu contul Artist creat pe acest dispozitiv. Autentificarea de producție va fi mutată ulterior pe server."}}as const;
export default function ArtistLogin(){const router=useRouter();const{locale}=useLanguage();const t=artistTranslations[locale],a=authTranslations[locale],m=messages[locale];const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");setBusy(true);const account=readArtistAccount();if(!account){setError(m.missing);setBusy(false);return}const hash=await hashArtistPassword(password);if(account.email.toLowerCase()!==email.trim().toLowerCase()||account.passwordHash!==hash){setError(m.invalid);setBusy(false);return}writeArtistSession(account);const next=new URLSearchParams(window.location.search).get("next");router.replace(next?.startsWith("/")?next:"/dashboard")}return <ArtistPortalShell><section className="page-wrap flex min-h-[68vh] items-center justify-center py-14"><form onSubmit={submit} className="w-full max-w-md rounded-[24px] border border-fuchsia-500/20 bg-[radial-gradient(circle_at_80%_0%,rgba(237,53,197,.18),transparent_32%),rgba(255,255,255,.025)] p-8 shadow-[0_0_55px_rgba(107,44,255,.08)]"><p className="page-kicker">{t.access}</p><h1 className="mt-3 text-4xl font-black tracking-[-.05em]">{t.loginTitle}</h1><p className="mt-3 text-sm text-zinc-400">{t.loginText}</p><label className="mt-7 block text-xs font-bold">{a.email}<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 p-4" placeholder="artist@example.com"/></label><label className="mt-4 block text-xs font-bold">{a.password}<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 p-4" placeholder="••••••••"/></label>{error&&<p className="mt-4 rounded-xl border border-rose-400/20 bg-rose-500/10 p-3 text-xs font-bold text-rose-200" role="alert">{error}</p>}<button type="submit" disabled={busy||!email.trim()||!password} className="m-primary mt-6 w-full disabled:opacity-40">{busy?m.working:t.login}</button><p className="mt-5 text-center text-[11px] leading-5 text-zinc-500">{m.note}</p><p className="mt-5 text-center text-xs text-zinc-400">{t.newArtist} <Link className="text-fuchsia-300" href="/for-artists/signup">{t.createAccount}</Link></p></form></section></ArtistPortalShell>}
