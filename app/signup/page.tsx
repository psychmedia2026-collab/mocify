"use client";import Link from"next/link";import{useRouter,useSearchParams}from"next/navigation";import{FormEvent,Suspense,useState}from"react";import{Shell}from"../components";import{hashListenerPassword,saveListenerAccount,saveListenerSession}from"../listener-account";import{useLanguage}from"../i18n/language-provider";import{listenerAuthCopy}from"../listener-auth-copy";
function SignupForm(){const router=useRouter(),params=useSearchParams(),selectedPlan=params.get("plan"),premium=Boolean(selectedPlan&&selectedPlan.toLowerCase()!=="gratis"&&selectedPlan.toLowerCase()!=="free"),{locale}=useLanguage(),t=listenerAuthCopy[locale],[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),displayName=String(f.get("displayName")||"").trim(),email=String(f.get("email")||"").trim().toLowerCase(),password=String(f.get("password")||""),country=String(f.get("country")||"NL"),language=String(f.get("language")||"nl");if(!displayName||!email||password.length<6){setError(t.badSignup);return}const passwordHash=await hashListenerPassword(password);saveListenerAccount({displayName,email,passwordHash,country,language,radioCountry:country,playlistCountry:country});saveListenerSession({email,displayName});router.push(premium?`/checkout?plan=${encodeURIComponent(selectedPlan!)}`:"/profile")}return <Shell player={false}><section className="page-wrap listener-auth"><form onSubmit={submit} className="listener-auth-card"><p className="page-kicker">{t.join}</p><h1>{t.createTitle}</h1><p>{premium?`Create your MOCIFY account first. You will then continue directly to the secure monthly payment for ${selectedPlan}.`:t.createText}</p>{premium&&<div className="listener-signup-plan"><span>Selected membership</span><strong>{selectedPlan}</strong><small>Monthly subscription · first payment during registration</small></div>}<label>{t.name}<input name="displayName" autoComplete="name" required/></label><label>{t.email}<input name="email" type="email" autoComplete="email" required/></label><label>{t.password}<input name="password" type="password" autoComplete="new-password" minLength={6} required/></label><div className="listener-auth-grid"><label>{t.country}<select name="country" defaultValue="NL"><option value="NL">🇳🇱 Netherlands</option><option value="RO">🇷🇴 România</option><option value="GB">🇬🇧 United Kingdom</option><option value="US">🇺🇸 United States</option><option value="DE">🇩🇪 Deutschland</option><option value="FR">🇫🇷 France</option><option value="ES">🇪🇸 España</option></select></label><label>{t.language}<select name="language" defaultValue={locale}><option value="nl">Nederlands</option><option value="en">English</option><option value="ro">Română</option><option value="de">Deutsch</option><option value="fr">Français</option><option value="es">Español</option><option value="it">Italiano</option><option value="pt">Português</option><option value="pl">Polski</option><option value="tr">Türkçe</option><option value="id">Bahasa Indonesia</option><option value="ja">日本語</option><option value="ko">한국어</option><option value="hi">हिन्दी</option></select></label></div>{error&&<p className="listener-auth-error" role="alert">{error}</p>}<button className="m-primary" type="submit">{premium?"Create account & continue to payment":t.create}</button><p>{t.already} <Link href="/login">{t.signIn}</Link></p></form></section></Shell>}
export default function Signup(){return <Suspense fallback={null}><SignupForm/></Suspense>}
