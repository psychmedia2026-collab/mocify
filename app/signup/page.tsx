"use client";import Link from"next/link";import{useRouter}from"next/navigation";import{FormEvent,useState}from"react";import{Shell}from"../components";import{hashListenerPassword,saveListenerAccount,saveListenerSession}from"../listener-account";
export default function Signup(){const router=useRouter(),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),displayName=String(f.get("displayName")||"").trim(),email=String(f.get("email")||"").trim().toLowerCase(),password=String(f.get("password")||""),country=String(f.get("country")||"NL"),language=String(f.get("language")||"nl");if(!displayName||!email||password.length<6){setError("Enter a name, valid email and a password of at least 6 characters.");return}const passwordHash=await hashListenerPassword(password);saveListenerAccount({displayName,email,passwordHash,country,language});saveListenerSession({email,displayName});router.push("/profile")}return <Shell player={false}><section className="page-wrap listener-auth"><form onSubmit={submit} className="listener-auth-card"><p className="page-kicker">JOIN MOCIFY</p><h1>Create listener account</h1><p>Build your library, playlists and following.</p><label>Display name<input name="displayName" required/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={6} required/></label><div className="listener-auth-grid"><label>Country<select name="country" defaultValue="NL"><option value="NL">🇳🇱 Netherlands</option><option value="RO">🇷🇴 Romania</option><option value="GB">🇬🇧 United Kingdom</option><option value="US">🇺🇸 United States</option><option value="DE">🇩🇪 Germany</option><option value="FR">🇫🇷 France</option><option value="ES">🇪🇸 Spain</option></select></label><label>Language<select name="language" defaultValue="nl"><option value="nl">Nederlands</option><option value="en">English</option><option value="ro">Română</option></select></label></div>{error&&<p className="listener-auth-error">{error}</p>}<button className="m-primary" type="submit">Create account</button><p>Already have an account? <Link href="/login">Sign in</Link></p></form></section></Shell>}